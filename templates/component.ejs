// @flow

import React from "react";
import { StyleSheet, View } from "react-native";

type Props = {};

export default function <%= props.name %>(props: Props) {
  return <View style={styles.container} />;
}

const styles = StyleSheet.create({
  container: {
    padding: 16
  }
});
